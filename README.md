# Ecommerce-Frontend

## Deployment

### Vercel
1. Connect your repository to Vercel.
2. Vercel will automatically detect the Vite project and use `.env.production`.
3. The `vercel.json` ensures that client-side routing works correctly.